function convertFahrToCelsius(fahrenheitToCelsius) 
{
  var temp = fahrenheit;
  var FahrToCelsius = C = (F - 32) * 5/9;
  var fToC = FahrToCelsius.toFixed(4);
  var convertFahrToCelsius = fTemp+'\xB0F is ' + fToCel + '\xB0C.';
    if (convertfahrToCelsius === ""  ) {
        console.log(convertFahrToCelsius)        
    } else {
        console.log("This is not a valid number but an array")
    } if (convertFahrToCelsius === '') {
        
    } else {
        console.log('[1,2,3] is not a valid number but an array')
        
    } {

    }
    }